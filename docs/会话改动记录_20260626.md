# 完整会话改动记录

> 日期：2026-06-26
> 涉及项目：crm-finetune + auto-carcrm

---

## 一、crm-finetune 项目改动

### 1.1 修改文件

#### `merge_model.py` — 适配器路径修正

```diff
- PEFT_MODEL = "./finetuned/LoRA_CRM"
+ PEFT_MODEL = "./finetuned/LoRA_CRM/checkpoint-200"
```

**原因**：adapter_model.safetensors 在 checkpoint-200 子目录，不在 LoRA_CRM 根目录。

#### `inference.py` — 测试用例替换

```diff
  test_questions = [
-     "比亚迪商用车的质保期是多久？",
-     "发动机故障灯亮了怎么办？",
-     "新能源商用车续航里程是多少？",
-     "我的比亚迪T5出现了发动机故障灯亮、动力不足，故障码P0101，该怎么办？",
-     "ABS故障灯亮了还能开吗？",
-     "水温报警怎么办？",
-     "维修需要多长时间？",
-     "路上抛锚怎么办？",
-     "多久保养一次？",
-     "保养需要做什么项目？",
+     # A. 训练集内（考察记忆）
+     "发动机故障灯亮了怎么办？",
+     "故障码P0562是什么意思？",
+     "比亚迪T5的参数配置是什么？",
+     # B. 训练集外（考察泛化）
+     "终身质保是保哪些零部件？",
+     "首保和定保有什么区别？",
+     "故障码P0300是什么意思？",
+     "刹车异响是什么原因？",
+     "空调不制冷怎么处理？",
+     # C. 边界题（考察鲁棒性）
+     "比亚迪T3的参数是什么？",
+     "我的车打不着火了怎么办？",
+     "你们公司还招人吗？",
  ]
```

### 1.2 新建文件

| 文件 | 用途 |
|------|------|
| `benchmark_simple.py` | 推理性能基准测试（TFTT/QPS/RPS），逐 token 计时 |
| `eval_report_gen.py` | 训练指标计算脚本，读取 trainer_state.json 生成报告 |
| `serve_openai.py` | OpenAI 兼容 API 服务器（FastAPI + 端口 8100，支持流式/非流式） |
| `docs/测试结果与部署方案.md` | 测试结果 + 面试话术 + 部署方案 |
| `docs/改动记录_部署接入RAG.md` | RAG 接入改动记录 |
| `docs/会话改动记录_20260626.md` | 本文件 |

### 1.3 安装依赖

```bash
pip install fastapi uvicorn sse-starlette
```

---

## 二、auto-carcrm 项目改动

### 2.1 修改文件

#### `.env` — LLM 配置切换

```diff
- LLM_DEFAULT_MODEL=mimo-v2.5-pro
+ LLM_DEFAULT_MODEL=qwen3-crm

- OPENAI_API_KEY=tp-c6uajm09kmtfjujxjnnkul0lhieb6t0j252put1vlbd4znbg
+ OPENAI_API_KEY=not-needed

- OPENAI_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1
+ OPENAI_BASE_URL=http://localhost:8100/v1

- VL_ENABLED=true
+ VL_ENABLED=false
```

**VL_ENABLED=false**：微调模型不支持视觉，关闭多模态。

### 2.2 新建文件

| 文件 | 用途 |
|------|------|
| `.env.backup` | 原始 .env 备份（回滚用） |

---

## 三、执行的命令汇总

```bash
# 1. 模型合并
powershell -Command "Set-Location 'D:\heimaAI\PytorchSDXX\10_微调\crm-finetune'; & '.\.venv\python.exe' merge_model.py"

# 2. 推理验证（11 题）
powershell -Command "Set-Location 'D:\heimaAI\PytorchSDXX\10_微调\crm-finetune'; & '.\.venv\python.exe' inference.py"

# 3. 评估指标计算
powershell -Command "Set-Location 'D:\heimaAI\PytorchSDXX\10_微调\crm-finetune'; & '.\.venv\python.exe' eval_report_gen.py"

# 4. 性能基准测试
powershell -Command "Set-Location 'D:\heimaAI\PytorchSDXX\10_微调\crm-finetune'; & '.\.venv\python.exe' benchmark_simple.py"

# 5. 安装 API 依赖
powershell -Command "& 'D:\heimaAI\PytorchSDXX\10_微调\crm-finetune\.venv\python.exe' -m pip install fastapi uvicorn sse-starlette"

# 6. 备份 RAG .env
cp "D:/heimaAI/PytorchSDXX/08_掌柜智库/实战/实战/auto-carcrm/.env" "D:/heimaAI/PytorchSDXX/08_掌柜智库/实战/实战/auto-carcrm/.env.backup"
```

---

## 四、产出物

| 文件 | 说明 |
|------|------|
| `model/Qwen3-4B-merged/` | 合并后的模型（部署用这个） |
| `eval_report.json` | 完整评估报告（训练指标 + 推理性能） |
| `docs/测试结果与部署方案.md` | 面试用文档 |
| `docs/改动记录_部署接入RAG.md` | RAG 接入改动 + 回滚方法 |

---

## 五、启动验证

```bash
# 终端 1：启动模型 API
cd D:\heimaAI\PytorchSDXX\10_微调\crm-finetune
.venv\python.exe serve_openai.py

# 终端 2：启动 RAG
cd D:\heimaAI\PytorchSDXX\08_掌柜智库\实战\实战\auto-carcrm
python app/main.py
```

---

## 六、回滚

```bash
# 切回小米 MiMo API
cp auto-carcrm/.env.backup auto-carcrm/.env
```
