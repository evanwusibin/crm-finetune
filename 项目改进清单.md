# CRM-Finetune 项目改进清单

> 📅 生成日期：2026-06-28
> 🎯 目的：明确项目与企业级标准的差距，给出分步改进指导
> 📊 评估基准：企业 AI 应用工程师岗位要求

---

## 一、项目现状评估

### 做得好的（优势）

| 维度 | 评价 | 企业对标 |
|------|------|----------|
| 完整链路 | 数据→训练→合并→推理→评估→部署→RAG 集成 | 超过多数初级项目 |
| 文档质量 | 11 篇 docs，含踩坑记录、面试话术 | 工程规范 |
| API 设计 | OpenAI 兼容接口（含流式 SSE） | 标准实践 |
| 测试设计 | 11 题分 3 类（记忆/泛化/边界） | 方法论正确 |
| 性能度量 | TFTT/QPS/RPS/tok/s 完整基准 | 有量化意识 |
| RAG 集成 | .env 切换即可对接下游 | 设计合理 |
| 工程诚实 | 自己标注了项目边界 | 面试加分 |

### 需要改进的（差距）

共 10 项，按严重程度分为三级：
- 🔴 Critical（影响模型效果，必须改）
- 🟡 Important（工程质量，应该改）
- 🟢 Nice-to-have（锦上添花）

---

## 二、逐项详细说明

---

### 🔴 1. 训练数据量严重不足（65 条）

#### 缺什么

训练集 52 条，测试集 13 条，总共 65 条。企业级微调通常需要数千到数万条。

#### 为什么缺

数据从 auto-carcrm 的 doc/data 手动转换，原始知识库本身就是小规模。课程未覆盖数据增强方法。

#### 好坏对比

```text
当前（65 条）：
  ✓ 能跑通微调链路
  ✓ 损失从 3.3 降到 1.2，有学习效果
  ✗ 泛化能力差（未见过的故障码答错）
  ✗ 面试时会被追问"65 条够吗"
  ✗ 实际业务中覆盖的故障码极其有限

扩充后（500+ 条）：
  ✓ 泛化能力显著提升
  ✓ 覆盖更多故障码和车型
  ✓ 面试时有底气说"这是经过充分训练的"
  ✗ 需要更多数据收集/生成时间
```

#### 对企业的影响

```text
- 模型只能回答训练过的 10 几个故障码
- 新车型/新故障码完全无法处理
- 实际业务中可用性很低
```

#### 对个人的影响

```text
- 面试时"65 条数据训出来的模型"说服力弱
- 无法展示"数据工程"能力
- 项目深度受限
```

#### 分步指导

```text
Step 1: 数据清洗（1 天）
  - 去重：检查 crm_train.jsonl 中是否有重复 QA 对
  - 格式统一：确保所有回答格式一致（markdown 结构化）
  - 标注质量：检查回答是否有事实错误

Step 2: 数据增强（2-3 天）
  方法 A：用 LLM 生成
    - 给 ChatGPT/Claude 看现有的 52 条数据
    - 让它按同样格式生成新故障码的 QA 对
    - 目标：每个车型 20-30 条，6 个车型 = 120-180 条

  方法 B：从知识库提取
    - auto-carcrm 的 PDF 文档里有更多故障码
    - 用脚本自动提取：故障码 + 含义 + 原因 + 解决方案
    - 转换成 QA 格式

  方法 C：复用维修案例
    - 每个维修案例可以拆成多个 QA 对
    - "P0562 故障码是什么" + "P0562 怎么修" + "P0562 维修费用多少"

Step 3: 数据扩充目标（1 周）
  - 原始数据：52 条
  - 增强后目标：200-500 条
  - 覆盖：每个车型 20+ 条、常见故障码 50+ 个

Step 4: 重新划分训练/测试集
  - 80% 训练，20% 测试
  - 确保测试集不和训练集重叠
```

---

### 🔴 2. MAX_LENGTH=128 太短

#### 缺什么

最大序列长度 128 tokens，维修案例的回答（含步骤、费用、工时）很容易超过 128，导致截断。

#### 为什么缺

默认值或为了省显存。128 在简单 QA 够用，但结构化长回答不够。

#### 好坏对比

```text
当前（MAX_LENGTH=128）：
  ✓ 显存占用小，训练快
  ✓ 简单问答（故障码含义）够用
  ✗ 维修案例回答被截断（步骤/费用/工时丢失）
  ✗ 推理测试中部分回答不完整

改为 512 或 1024：
  ✓ 完整保留维修案例的回答
  ✓ 推理时回答更完整
  ✗ 显存占用增加（需要评估是否放得下）
  ✗ 训练时间增加
```

#### 显存估算

```text
Qwen3-4B BF16：
  模型本身：~8GB
  MAX_LENGTH=128：训练时显存 ~10GB
  MAX_LENGTH=512：训练时显存 ~12GB（RTX 5070Ti 刚好能跑）
  MAX_LENGTH=1024：训练时显存 ~14GB（可能 OOM）

建议：先试 512，如果 OOM 就用 gradient checkpointing
```

#### 分步指导

```text
Step 1: 修改 train_qlora.py
  MAX_LENGTH = 512  # 原来是 128

Step 2: 检查显存
  训练时观察 GPU 显存占用
  如果 OOM：加 gradient_checkpointing=True

Step 3: 重新训练
  观察 loss 曲线和推理效果

Step 4: 验证
  用之前截断的测试题重新测试
```

---

### 🔴 3. Qwen3 Thinking 模式未关闭

#### 缺什么

Qwen3 默认启用 thinking 模式（生成 `<think>...</think>` 标签内的推理过程），在 MAX_LENGTH=128 下 thinking 就消耗完了 token 额度，导致实际回答为空。

#### 为什么缺

Qwen3 新特性，老师可能未提前说明。默认行为和 Qwen2 不同。

#### 好坏对比

```text
当前（thinking 开启）：
  ✓ 模型会"思考"再回答（理论上更准确）
  ✗ 128 tokens 被 thinking 吃完，回答为空
  ✗ 推理延迟增加（thinking 需要额外 token）
  ✗ 用户看到的是空回答或截断回答

关闭 thinking：
  ✓ 直接回答，token 全用在有用内容上
  ✓ 推理速度更快
  ✓ 回答完整
  ✗ 可能损失一些推理质量（但对简单 QA 影响小）
```

#### 分步指导

```text
Step 1: 在 system prompt 末尾加 /no_think
  "你是比亚迪商用车智能售后助手。/no_think"

Step 2: 或在生成参数中设置
  # 如果用 vLLM
  vllm serve ... --chat-template-no-think

  # 如果用 Transformers
  tokenizer.apply_chat_template(messages, add_generation_prompt=True)

Step 3: 重新测试
  用之前的 11 题重新评估
```

---

### 🟡 4. vLLM 未实际部署

#### 缺什么

`serve_openai.py` 用 FastAPI + `model.generate()` 串行生成，无并发能力。vLLM 只在文档中描述，未实际部署。

#### 为什么缺

vLLM 部署需要额外配置，课程可能未覆盖。FastAPI serve 是最简单的方案。

#### 对比

```text
当前（FastAPI serve）：
  ✓ 代码简单，一个文件搞定
  ✓ 能跑，TFTT 50ms
  ✗ 单请求串行，无法并发
  ✗ 无 continuous batching
  ✗ 无 PagedAttention（显存利用率低）
  ✗ 10 个并发请求 = 排队

vLLM 部署：
  ✓ continuous batching（动态合并请求）
  ✓ PagedAttention（显存利用率高）
  ✓ 支持 10-50 并发
  ✓ 推理吞吐量提升 3-5 倍
  ✓ OpenAI 兼容 API（自带）
  ✗ 需要额外安装 vLLM
  ✗ 需要配置启动参数
```

#### 分步指导

```text
Step 1: 安装 vLLM
  pip install vllm

Step 2: 启动 vLLM 服务
  vllm serve model/Qwen3-4B-merged \
    --host 0.0.0.0 --port 8100 \
    --served-model-name qwen3-crm \
    --max-model-len 2048 \
    --chat-template-no-think

Step 3: 验证
  curl http://localhost:8100/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{"model":"qwen3-crm","messages":[{"role":"user","content":"P0562是什么故障码"}]}'

Step 4: 更新 RAG 项目配置
  OPENAI_BASE_URL=http://localhost:8100/v1（端口不变，无缝切换）
```

---

### 🟡 5. LoRA 配置偏保守

#### 缺什么

LoRA rank=4（偏小），只 target `q_proj` + `v_proj`（未覆盖 K/O/FFN），alpha/r=1.0。

#### 为什么缺

保守配置为了省显存、快训练。课程可能推荐了最小可用配置。

#### 对比

```text
当前（r=4, q+v only）：
  ✓ 可训练参数少（1.9M，0.048%）
  ✓ 训练快
  ✓ 显存占用小
  ✗ 表达能力有限
  ✗ 复杂模式学不到

建议配置（r=16, all attention）：
  ✓ 表达能力更强
  ✓ 可以学到更多领域模式
  ✗ 可训练参数增加（~10M）
  ✗ 训练时间增加 2-3 倍
  ✗ 显存增加约 1GB
```

#### 分步指导

```text
Step 1: 修改 LoRA 配置
  LORA_R = 16          # 原来是 4
  LORA_ALPHA = 32      # 原来是 4（alpha = 2*r）
  TARGET_MODULES = ["q_proj", "k_proj", "v_proj", "o_proj"]
  # 如果显存够，加上 MLP:
  # TARGET_MODULES = ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]

Step 2: 重新训练
  观察 loss 曲线是否更好

Step 3: 对比推理效果
  用相同的测试集对比 r=4 和 r=16 的回答质量
```

---

### 🟡 6. 无自动化测试

#### 缺什么

没有 pytest 单元测试，没有集成测试，推理评估是手动的。

#### 分步指导

```text
Step 1: 写单元测试
  - test_prepare_data.py：数据格式转换正确性
  - test_merge_model.py：合并后模型能加载

Step 2: 写集成测试
  - test_inference.py：输入测试题，检查输出非空且包含关键词
  - test_api.py：调用 /v1/chat/completions，检查响应格式

Step 3: 加入 CI
  - GitHub Actions 跑 pytest
```

---

### 🟡 7. 训练日志不够结构化

#### 缺什么

TensorBoard 日志只有 loss 和 accuracy，缺少学习率、梯度范数、显存占用等关键指标。

#### 分步指导

```text
Step 1: 在训练循环中额外记录
  - 学习率变化
  - 梯度范数（检测梯度爆炸）
  - GPU 显存占用
  - 每步耗时

Step 2: 可视化
  TensorBoard 中对比 train_loss vs eval_loss
```

---

### 🟢 8. 无数据版本管理

#### 缺什么

训练数据没有版本控制，改了数据不知道改了什么。

#### 分步指导

```text
Step 1: 数据文件加版本号
  data/crm_train_v1.jsonl
  data/crm_train_v2.jsonl

Step 2: 记录每次训练用的数据版本
  在 eval_report.json 中加 data_version 字段
```

---

### 🟢 9. 无 Dockerfile

#### 缺什么

微调项目没有容器化，无法快速复现训练环境。

#### 分步指导

```text
Step 1: 创建 Dockerfile
  FROM pytorch/pytorch:2.3.0-cuda12.1-cudnn8-runtime
  COPY requirements.txt .
  RUN pip install -r requirements.txt
  COPY . /workspace
  WORKDIR /workspace

Step 2: 创建 docker-compose.yml
  服务：train（训练）、serve（推理）
  卷挂载：model/、finetuned/、data/
```

---

### 🟢 10. 无 A/B 测试框架

#### 缺什么

无法量化对比不同 LoRA 配置、不同数据量、不同超参数的效果。

#### 分步指导

```text
Step 1: 定义实验记录格式
  {
    "experiment_id": "r16_alpha32_500data",
    "lora_r": 16,
    "lora_alpha": 32,
    "train_samples": 500,
    "max_length": 512,
    "results": {"accuracy": 0.82, "tftt": 45}
  }

Step 2: 每次训练后自动记录
  保存到 experiments/ 目录

Step 3: 对比不同实验
  生成对比表格
```

---

## 三、改进优先级排序

### 第一批：面试前必须改（2-3 天）

```text
1. MAX_LENGTH 改成 512（改一行代码，重新训练）
2. 关闭 Qwen3 thinking 模式（加 /no_think）
3. 数据扩充到 200+ 条（用 LLM 生成）
4. 用 vLLM 替代 FastAPI serve（一条命令启动）
```

### 第二批：简历上可以写（1 周）

```text
5. LoRA 配置升级（r=16, alpha=32, all attention）
6. 写自动化测试（pytest）
7. 结构化训练日志
8. 创建 Dockerfile
```

### 第三批：有时间再搞（2 周）

```text
9. 数据版本管理
10. A/B 测试框架
11. CI/CD（GitHub Actions）
12. 多 LoRA 实验对比报告
```

---

## 四、面试话术

### Q: 你用了多少数据微调？

> "训练集 52 条，测试集 13 条，总共 65 条。这是最小验证版本，用于验证微调链路是否跑通。实际生产中我会扩充到 500+ 条，用 LLM 基于现有数据生成更多故障码的 QA 对，覆盖每个车型 20+ 条。65 条已经能验证 loss 从 3.3 降到 1.2，证明 LoRA 微调方向是对的。"

### Q: 为什么选 LoRA 不选全参微调？

> "RTX 5070Ti 只有 12GB 显存，Qwen3-4B BF16 本身就占 8GB，全参微调需要额外 16GB+ 显存，根本放不下。LoRA 只训练 1.9M 参数（0.048%），显存占用小，训练快，30 分钟跑完 200 步。效果上 loss 从 3.3 降到 1.2，token accuracy 68.5%，对 65 条数据的小数据集来说已经验证了可行性。"

### Q: 你打算怎么改进这个项目？

> "四个方向：1) 数据扩充到 200+ 条提升泛化能力；2) MAX_LENGTH 从 128 改到 512 解决回答截断；3) 用 vLLM 替代 FastAPI serve 支持并发推理；4) 关闭 Qwen3 thinking 模式让 token 全用在有用内容上。这些改完后，模型的实用性和部署能力都能达到企业标准。"

---

📁 **输出位置**：`D:\heimaAI\PytorchSDXX\10_微调\crm-finetune\项目改进清单.md`
