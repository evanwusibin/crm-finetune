# 微调项目改动记录

> 📅 改动日期：2026-06-30
> 📁 涉及文件：train_qlora.py, crm_train.jsonl, crm_test.jsonl, fix_qwen3_thinking.py

---

## 一、改动汇总

| # | 改动项 | 修改前 | 修改后 | 所在文件 |
|---|--------|--------|--------|----------|
| 1 | MAX_LENGTH | 128 | **512** | train_qlora.py:32 |
| 2 | LoRA rank | r=4, alpha=4 | **r=16, alpha=32** | train_qlora.py:35-36 |
| 3 | Target modules | q_proj, v_proj | **q_proj, k_proj, v_proj, o_proj** | train_qlora.py:37 |
| 4 | Qwen3 thinking | 开启（浪费 token） | **关闭（/no_think）** | crm_train.jsonl, crm_test.jsonl |
| 5 | Gradient checkpointing | 无 | **True** | train_qlora.py:134 |
| 6 | 路径处理 | 相对路径 | **绝对路径** | train_qlora.py:21-25 |

---

## 二、逐项改动说明

### 1. MAX_LENGTH：128 → 512

**文件**：`train_qlora.py` 第 32 行

```python
# 修改前
MAX_LENGTH = 128

# 修改后
MAX_LENGTH = 512  # 修复截断，RTX 5070Ti 12GB 可承受
```

**原因**：MAX_LENGTH=128 导致维修案例的长回答被截断（步骤、费用、工时丢失），推理测试中部分回答不完整。

**显存估算**：
- MAX_LENGTH=128：训练显存 ~10GB
- MAX_LENGTH=512：训练显存 ~12GB（RTX 5070Ti 刚好能跑）
- MAX_LENGTH=1024：显存 ~14GB（可能 OOM）

---

### 2. LoRA 配置升级

**文件**：`train_qlora.py` 第 35-38 行

```python
# 修改前
LORA_R = 4
LORA_ALPHA = 4
TARGET_MODULES = ["q_proj", "v_proj"]

# 修改后
LORA_R = 16
LORA_ALPHA = 32  # alpha = 2*r
TARGET_MODULES = ["q_proj", "k_proj", "v_proj", "o_proj"]
```

**原因**：
- r=4 表达能力有限，复杂模式学不到
- 只 target q+v 遗漏了 K/O/FFN 层的信息
- 升级后可训练参数从 1.9M 增加到 ~10M，表达能力更强

---

### 3. 关闭 Qwen3 Thinking 模式

**文件**：`data/crm_train.jsonl` 和 `data/crm_test.jsonl`

```python
# 修改前
{"role": "system", "content": "你是比亚迪商用车智能售后助手..."}

# 修改后
{"role": "system", "content": "你是比亚迪商用车智能售后助手... /no_think"}
```

**原因**：Qwen3 默认启用 thinking 模式（生成 `<think>...</think>` 标签），在 MAX_LENGTH=128 下 thinking 就消耗完了 token 额度，导致实际回答为空。

**修复脚本**：`fix_qwen3_thinking.py`（自动生成，已执行）

---

### 4. 添加 Gradient Checkpointing

**文件**：`train_qlora.py` 第 134 行

```python
# 修改前
training_args = SFTConfig(
    ...
)

# 修改后
training_args = SFTConfig(
    ...
    gradient_checkpointing=True,  # 新增：防 OOM
    ...
)
```

**原因**：MAX_LENGTH=512 时显存占用增加，gradient checkpointing 以时间换空间，避免 OOM。

---

### 5. 路径处理：相对路径 → 绝对路径

**文件**：`train_qlora.py` 第 21-25 行

```python
# 修改前
MODEL_PATH = "./model/Qwen3-4B"
DATA_DIR = "./data"
OUTPUT_DIR = "./finetuned/LoRA_CRM"
LOG_DIR = "./logs/LoRA_CRM"

# 修改后
PROJECT_DIR = os.path.dirname(os.path.abspath(__file__))
MODEL_PATH = os.path.join(PROJECT_DIR, "model", "Qwen3-4B")
DATA_DIR = os.path.join(PROJECT_DIR, "data")
OUTPUT_DIR = os.path.join(PROJECT_DIR, "finetuned", "LoRA_CRM")
LOG_DIR = os.path.join(PROJECT_DIR, "logs", "LoRA_CRM")
```

**原因**：相对路径在不同工作目录下运行会报错 FileNotFoundError。

---

## 三、训练参数对比

| 参数 | 修改前 | 修改后 |
|------|--------|--------|
| MAX_LENGTH | 128 | **512** |
| LORA_R | 4 | **16** |
| LORA_ALPHA | 4 | **32** |
| TARGET_MODULES | q_proj, v_proj | **q_proj, k_proj, v_proj, o_proj** |
| gradient_checkpointing | False | **True** |
| Qwen3 thinking | 开启 | **关闭** |
| BATCH_SIZE | 1 | 1 |
| GRAD_ACCUM | 32 | 32 |
| MAX_STEPS | 500 | 500 |
| LEARNING_RATE | 2e-5 | 2e-5 |

---

## 四、需要重新训练

修改已就绪，GPU 可用时运行：

```bash
cd D:\heimaAI\PytorchSDXX\10_微调\crm-finetune
python train_qlora.py
```

---

## 五、后续待办

| 优先级 | 事项 | 预计时间 |
|--------|------|----------|
| 🔴 高 | 数据扩充到 200+ 条 | 2-3 天 |
| 🔴 高 | vLLM 部署替代 FastAPI serve | 1 天 |
| 🟡 中 | 评估新 LoRA 配置效果 | 1 天 |
| 🟡 中 | 自动化测试（pytest） | 1 天 |

---

📁 **文档位置**：`D:\heimaAI\PytorchSDXX\10_微调\crm-finetune\改动记录_20260630.md`
