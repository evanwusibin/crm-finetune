# CRM 汽车售后知识助手 · 微调实战指南

> **目标**：用你 auto-carcrm 项目的 FAQ + 维修案例 + 故障码数据，微调 Qwen3-8B 模型
> **方法**：QLoRA（4bit 量化 + LoRA，RTX 5070 12GB 够用）
> **参考**：尚硅谷微调教程 sz_0302_llm

---

## 整体流程

```
Step 1：环境准备（conda + 依赖）
Step 2：下载模型（Qwen3-8B）
Step 3：准备数据（CRM 项目数据 → 微调格式）
Step 4：QLoRA 微调（核心步骤）
Step 5：合并模型（LoRA 适配器 → 基座模型）
Step 6：推理验证（测试微调效果）
Step 7：部署 API（可选，FastAPI 服务）
```

---

## Step 1：环境准备

```bash
# 进入项目目录
cd D:\heimaAI\PytorchSDXX\10_微调\crm-finetune

# 创建本地 conda 环境（装在项目目录下，不污染全局）
conda create --prefix ./.venv python=3.12 -y

# 激活环境
conda activate D:\heimaAI\PytorchSDXX\10_微调\crm-finetune\.venv

# 安装 PyTorch（CUDA 12.4）
conda install pytorch torchvision pytorch-cuda=12.4 -c pytorch -c nvidia -y

# 安装微调依赖
pip install transformers datasets peft trl bitsandbytes accelerate tensorboard modelscope

# 验证 CUDA
python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
# 预期输出：2.x.x+cu124 True
```

> **如果 conda 安装 PyTorch 太慢**，可以先用 pip：
> ```bash
> pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
> ```
> 如果报代理错误，先关代理：`set HTTP_PROXY= && set HTTPS_PROXY=`

---

## Step 2：下载模型

```bash
# 仍在项目目录下，激活环境后执行
python -c "
from modelscope import snapshot_download
snapshot_download('Qwen/Qwen3-8B', local_dir='model/Qwen3-8B')
print('下载完成')
"
```

> 模型约 16GB，下载需要一些时间。
> 如果 modelscope 下载慢，也可以用 huggingface：
> ```bash
> pip install huggingface_hub
> huggingface-cli download Qwen/Qwen3-8B --local-dir model/Qwen3-8B
> ```

---

## Step 3：准备数据

把 CRM 项目的 FAQ + 维修案例 + 故障码转成微调格式。

**运行数据转换脚本：**
```bash
python prepare_data.py
```

> 脚本会生成 `data/crm_train.jsonl` 和 `data/crm_test.jsonl`

---

## Step 4：QLoRA 微调（核心步骤）

**运行微调脚本：**
```bash
python train_qlora.py
```

**训练过程监控（另开一个终端）：**
```bash
conda activate D:\heimaAI\PytorchSDXX\10_微调\crm-finetune\.venv
tensorboard --logdir logs/
# 浏览器打开 http://localhost:6006 查看 loss 曲线
```

> 训练时间：RTX 5070 上大约 30-60 分钟（1000 步）
> 显存占用：约 8-10GB（12GB 够用）

---

## Step 5：合并模型

LoRA 训练出来的是"适配器"，需要合并到基座模型才能部署。

```bash
python merge_model.py
```

> 合并后的模型保存在 `model/Qwen3-8B-merged/`

---

## Step 6：推理验证

```bash
python inference.py
```

> 测试微调后的模型是否学会了 CRM 问答风格

---

## Step 7：部署 API（可选）

```bash
pip install fastapi uvicorn
python serve_api.py
```

> 访问 http://localhost:8000/docs 查看 API 文档

---

## 常见问题

**Q1：CUDA Out of Memory**
```bash
# 在 train_qlora.py 中减小 batch_size
per_device_train_batch_size=2  # 从 4 改成 2
gradient_accumulation_steps=16  # 相应增大，保持总 batch 不变
```

**Q2：loss 不下降**
- 检查数据格式是否正确（messages 格式）
- 检查学习率（QLoRA 推荐 3e-4）
- 检查数据质量

**Q3：训练后效果不好**
- 增加训练数据量
- 增加训练步数（max_steps）
- 调整 LoRA 的 r 参数（从 4 改成 8 或 16）

---

## 文件结构

```
crm-finetune/
├── .venv/                    # 本地 conda 环境
├── model/
│   ├── Qwen3-8B/            # 基座模型
│   └── Qwen3-8B-merged/     # 合并后的模型
├── data/
│   ├── crm_train.jsonl      # 训练数据
│   └── crm_test.jsonl       # 测试数据
├── finetuned/
│   └── QLoRA_CRM/           # LoRA 适配器
├── logs/                     # TensorBoard 日志
├── prepare_data.py           # 数据转换脚本
├── train_qlora.py            # 微调脚本
├── merge_model.py            # 模型合并脚本
├── inference.py              # 推理验证脚本
├── serve_api.py              # API 部署脚本
└── 微调实战指南.md            # 本文件
```
